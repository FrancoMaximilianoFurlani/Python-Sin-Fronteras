// script.js

// Aquí puedes agregar lógica para obtener datos o generar el informe dinámicamente
// Por ahora, simplemente vamos a agregar un texto de ejemplo al contenedor del informe.

document.addEventListener("DOMContentLoaded", function () {
    var reportContainer = document.getElementById("reportContainer");
    reportContainer.innerHTML = "<p>Este es un informe de ejemplo.</p>";
});
